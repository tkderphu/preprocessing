# Feature extract data from docs/pdf using Apache tika and Tesseract OCR

# Tech

Using python write a method to extract data from a docs/pdf files using Apache Tika and Tesseract OCR. 

When extracting data, if the file is a pdf or docs, if the file contain text then use Apache Tika to extract text from the file, if the file contain image then use Tesseract OCR to extract text from the file. 

In both cases return the text from the file.

The final result will be a text file